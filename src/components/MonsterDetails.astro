---
import MonsterReveal from './react/MonsterReveal'
interface Props {
  title?: string;
}

const { title = "Click to reveal" } = Astro.props;
---

<MonsterReveal client:only title={title}>
  <slot />
</MonsterReveal>
