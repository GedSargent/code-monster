---
import type { SandpackFiles, SandpackSetup } from '@codesandbox/sandpack-react/types';
import CodePlayground from './react/CodePlayground';

interface Props {
  localStorageId: string;
  customSetup?: SandpackSetup;
  template?:
    | "react-ts"
    | "react"
    | "vanilla"
    | "vanilla-ts"
    | "vue"
    | "vue-ts"
    | "angular"
    | "svelte"
    | "solid"
    | "test-ts";
  title?: string;
  files: SandpackFiles;
  activeFile?: string;
  showTabs?: boolean;
  showConsole?: boolean;
}

const {
  customSetup,
  localStorageId,
  title = "Code Playground",
  template = "react-ts",
  files,
  showConsole = false,
  showTabs = true,
} = Astro.props
---

<CodePlayground
  client:load
  localStorageId={localStorageId}
  files={files}
  customSetup={customSetup}
  template={template}
  title={title}
  showConsole={showConsole}
  showTabs={showTabs}
/>
