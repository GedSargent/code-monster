---
import Reveal from './react/MonsterReveal'
interface Props {
  title?: string;
  secretRevealCode?: string;
}

const { title = "Click to reveal", secretRevealCode } = Astro.props;
---

<Reveal client:load title={title} secretRevealCode={secretRevealCode}>
  <slot />
</Reveal>
