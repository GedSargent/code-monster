const ResizeHandle = () => {
  return (
    <button
      className="hidden
    lg:block
    lg:cursor-col-resize
    lg:pointer-events-auto
    lg:z-10
    lg:w-2
    lg:h-full
    lg:relative
    lg:bg-[#2D2A2E]
    lg:hover:bg-neutral-600
    "
    />
  );
};

export default ResizeHandle;
